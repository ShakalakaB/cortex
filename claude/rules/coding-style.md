# Coding Style

---

## Single-Loop Data Extraction

+ When extracting multiple pieces of data from a collection (e.g., keys, plucked values, keyed maps), build them all in a single loop instead of iterating over the collection multiple times with separate operations.
